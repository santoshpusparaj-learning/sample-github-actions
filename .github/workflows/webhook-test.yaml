name: VMSS Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: [self-hosted, azure-vmss]

    steps:
      - name: Test runner
        run: |
          echo "VMSS runner started m"
          hostname
          sleep 50